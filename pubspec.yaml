name: core_module
description: "A new Flutter package project."
version: 0.0.1

publish_to: "none"

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  dependencies_module:
    git:
      url: https://github.com/WillOliveiraa/will_store_v2_dependencies_module
      ref: main

dependency_overrides:
  dependencies_module:
    path: ../dependencies_module

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/languages/
